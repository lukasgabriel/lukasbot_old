
# lukasbot

 A custom discord bot for my discord server, using discord.py

 I'm continuously adding our own commands and experimenting.

---

## The default command prefix is '>'

| Command            |  Description
|--------------------|--------------------------------------------------------------------------------------------------|
| **`about`**        |  Displays the bot's about message  .                                                             |
| **`addgame`**      |  Add a game suggestion to the list of games that the `whattoplay` command randomly chooses from. |
| **`addresses`**    |  Displays the bot's address book with phone numbers used by the `sms` command.                   |
| **`bye`**          |  Displays 'goodbye' message.                                                                     |
| **`cat`**          |  Uses the 'thisapidoesnotexist' API to display a picture of a cat generated by a GAN.            |
| **`goodbot`**      |  Currently only displays 'thank you' message. More functionality planned.                        |
| **`hello`**        |  Displays 'hello' message.                                                                       |
| **`help`**         |  Displays list of available commands. Use a command as argument for detailed help.               |
| **`listcommands`** |  Currently not working.                                                                          |
| **`person`**       |  Uses the 'thisapidoesnotexist' API to display a picture of a person generated by a GAN.         |
| **`sms`**          |  Send an SMS. Format your request like so:  MESSAGE : RECIPIENT.                                 |
| **`vibecheck`**    |  Fluff function. Currently, you can't fail a vibe check.                                         |
| **`whattoplay`**   |  Displays a random game suggestion from a list. You can add to it via `addgame`.                 |
